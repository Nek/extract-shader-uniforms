const config = {
    entries: [
        {
            filePath: "./src/index.ts",
            outFile: "./dist/extract-shader-uniforms.d.ts",
            noCheck: false,
        },
    ],
};

module.exports = config;
